const express = require('express');
const emailUpdate = require('../Controller/emailUpdateController');

const router = express.Router();

//router.route('/send-Email').post(emailUpdate.sendUpdateEmail);

module.exports=router;